{{- with $.Values.api }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "crypto-trade.fullname" $ }}-api
  labels:
    {{- include "crypto-trade.labels" $ | nindent 4 }}
data:
  ENV: {{ .env }}
{{ end }}