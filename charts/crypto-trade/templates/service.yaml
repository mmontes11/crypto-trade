apiVersion: v1
kind: Service
metadata:
  name: {{ include "crypto-trade.fullname" . }}
  labels:
    {{- include "crypto-trade.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crypto-trade.selectorLabels" . | nindent 4 }}
